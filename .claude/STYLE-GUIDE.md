# VibeBlog 风格指南 — 基于 skillsmp.com 设计语言

> 从 skillsmp.com 提取的设计模式，适配 vibeblog 各页面的统一风格模板。

---

## 1. 设计理念

**终端/开发者美学 (Terminal-Inspired Developer Aesthetic)**

整体风格以"代码编辑器 + 终端"为隐喻，将 UI 元素伪装成代码结构，营造开发者熟悉的沉浸感。

核心原则：
- 代码即界面：用 `export`、`import`、`//`、`$` 等语法元素装饰内容
- 终端窗口卡片：所有卡片组件统一使用"终端窗口"样式（红黄绿圆点 + 文件名标题栏）
- 等宽字体 UI 镶边：导航、标题栏、标签等 UI 元素使用等宽字体，正文内容使用无衬线字体
- 玻璃态层次：卡片使用 `backdrop-filter: blur()` + 半透明背景，营造层次感

---

## 2. 色彩体系

已在 `tokens.css` 中定义，核心色值：

| 用途 | Light | Dark | Token |
|------|-------|------|-------|
| 主色 (紫) | `#8b5cf6` | `#a78bfa` | `--color-primary` |
| 功能蓝 | `#3b82f6` | `#60a5fa` | `--color-syntax-function` |
| 字符串绿 | `#22c55e` | `#4ade80` | `--color-syntax-string` |
| 变量粉 | `#ec4899` | `#f472b6` | `--color-syntax-variable` |
| 关键字紫 | `#8b5cf6` | `#a78bfa` | `--color-syntax-keyword` |
| 注释灰 | `#64748b` | `#64748b` | `--color-syntax-comment` |
| 数字橙 | `#f59e0b` | `#fbbf24` | `--color-syntax-number` |

### 语法高亮映射规则
- 标题/导出名 → `--color-text-primary` (正文色)
- 关键字 (`export`, `import`, `from`) → `--color-syntax-keyword` (紫)
- 路径/字符串 → `--color-syntax-string` (绿)
- 链接/函数 → `--color-syntax-function` (蓝)
- 注释 (`//`) → `--color-syntax-comment` (灰斜体)
- 变量/装饰符 (`@`) → `--color-syntax-variable` (粉)
- 数字/统计 → `--color-syntax-number` (橙)
- 命令提示符 (`$`, `❯`) → `--color-primary` (紫)

---

## 3. 字体体系

```css
--font-mono: 'JetBrains Mono', 'Fira Code', 'Courier New', monospace;
--font-sans: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
```

| 场景 | 字体 | 示例 |
|------|------|------|
| 导航栏文字 | `--font-mono` | `$ ai --search` |
| 卡片标题栏 | `--font-mono` | `feature-flags.md` |
| 标签/Badge | `--font-mono` | `BLOG`, `XHS` |
| 代码行内容 | `--font-mono` | `export Vue3入门` |
| 正文内容 | `--font-sans` | 博客正文段落 |
| 按钮文字 | `--font-mono` | `execute` |

---

## 4. 组件模式

### 4.1 终端窗口卡片 (Terminal Card)

所有主要卡片统一使用此模式：

```
┌─ ● ● ● ─── filename.md ──────────────────┐
│  (卡片内容区域)                              │
│                                              │
└──────────────────────────────────────────────┘
```

CSS 结构：
```css
.terminal-card {
  background: var(--color-bg-card);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);  /* 12px */
  overflow: hidden;
}

.terminal-card-header {
  display: flex;
  align-items: center;
  gap: var(--space-md);
  padding: var(--space-sm) var(--space-md);
  background: var(--color-muted);
  border-bottom: 1px solid var(--color-border);
}

.terminal-dots {
  display: flex;
  gap: var(--space-xs);
}

.terminal-dot { width: 12px; height: 12px; border-radius: 50%; }
.terminal-dot.red { background: var(--color-dot-red); }
.terminal-dot.yellow { background: var(--color-dot-yellow); }
.terminal-dot.green { background: var(--color-dot-green); }

.terminal-title {
  font-family: var(--font-mono);
  font-size: var(--font-size-xs);
  color: var(--color-text-tertiary);
}
```

### 4.2 玻璃态卡片 (Glass Card)

用于浮动面板、侧边栏卡片：

```css
.glass-card {
  background: var(--glass-bg);  /* rgba(255,255,255,0.85) */
  backdrop-filter: blur(12px);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
}
```

### 4.3 代码风格内容卡片 (Code Content Card)

用于博客列表、技能列表等网格卡片：

```
┌─ 📁 blog-posts ──── ● module ─┐
│ 1  export 文章标题              │
│ 2  @ from "blog/generator"     │
│ 3  // 深度技术教程              │
│ ─────────────────────────────  │
│ $$ cat blog.md                 │
├────────────────────────────────┤
│ [BLOG] [📖 4] [💻 2]    3天前  │
└────────────────────────────────┘
```

### 4.4 导航栏 (Navigation)

命令行风格导航：

```
<vibe-blog />    [$ cd /categories] [$ ai --search] [$ watch stats]  🌙
```

- Logo: 等宽字体 + 渐变色 (`--color-primary-gradient`)
- 导航项: `$ command --flag` 格式
- 活跃项: `bg-primary/10 + border-primary/20`
- 玻璃态背景: `backdrop-filter: blur(12px)`

### 4.5 面包屑 (Breadcrumb)

路径风格：
```
$ pwd: ~ / docs / api
```

### 4.6 统计栏 (Stats Bar)

```
❯ count: 42 篇博客  --sort-by  [⭐ stars] [🕐 recent] [📊 views]
```

### 4.7 搜索框 (Search)

终端窗口内嵌搜索：
```
┌─ ● ● ● ─── search.sh ─── [clear] ─┐
│  ❯ find  [搜索 42 篇博客...]        │
└─────────────────────────────────────┘
```

---

## 5. 页面风格映射

### 5.1 首页 (Home.vue) ✅ 已实现

| 元素 | 当前状态 | 风格要求 |
|------|---------|---------|
| HeroSection | ✅ `> Create Amazing Blogs` + 光标闪烁 | 终端命令风格 |
| BlogInputCard | ✅ 终端窗口 (红黄绿圆点 + `$ find`) | 完整终端卡片 |
| BlogHistoryList | ✅ `$ ls ~/history` + 代码风格卡片 | 代码内容卡片网格 |
| AppNavbar | ✅ 玻璃态 + 等宽字体 Logo | 命令行导航 |
| 背景 | ✅ 代码符号粒子动画 | 微妙的代码粒子 |

### 5.2 博客列表 (BlogList.vue) ✅ 已实现

| 元素 | 当前状态 | 风格要求 |
|------|---------|---------|
| 导航 | ✅ `$ cd /categories` + `$ ai --search` | 命令行导航 |
| 搜索 | ✅ 终端窗口 (`search.sh` + `❯ find`) | 终端搜索框 |
| 统计栏 | ✅ `❯ count: N --sort-by` | 命令行统计 |
| 卡片网格 | ✅ 代码风格 (`export` + `import` + `//`) | 代码内容卡片 |

### 5.3 生成页面 (Generate.vue) ⚠️ 部分实现

| 元素 | 当前状态 | 风格要求 |
|------|---------|---------|
| 背景 | ✅ 渐变背景 | `linear-gradient(135deg, ...)` |
| research-card | ✅ 玻璃态 + `backdrop-filter` | 终端窗口卡片 |
| 预览面板 | ✅ 链接蓝色 + 代码粉色 | 统一语法高亮 |
| 进度面板 | ⚠️ 缺少终端窗口头部 | 应加终端圆点 + 标题 |
| 面包屑 | ❌ 无 | 应加 `$ pwd: ~ / generate / {task_id}` |

### 5.4 博客详情 (BlogDetail.vue) ✅ 已实现

| 元素 | 当前状态 | 风格要求 |
|------|---------|---------|
| 背景 | ✅ 渐变背景 | 与 Generate 一致 |
| content-card | ✅ 终端窗口 (圆点 + 文件名) | 完整终端卡片 |
| sidebar-card | ✅ 玻璃态 + JSON 风格 | 命令行侧边栏 |
| 引用卡片 | ✅ 终端窗口头部 + 玻璃态 | CitationTooltip |

### 5.5 任务中心 (Dashboard.vue) ❌ 未实现

| 元素 | 当前状态 | 风格要求 |
|------|---------|---------|
| 导航 | ⚠️ 使用 AppNavbar | 应统一命令行风格 |
| 统计卡片 | ❌ 普通卡片 | 应改为终端窗口卡片 |
| 任务列表 | ❌ 普通列表 | 应改为代码风格卡片 |
| 进度条 | ❌ 普通进度条 | 可加终端风格装饰 |

### 5.6 其他页面

| 页面 | 状态 | 备注 |
|------|------|------|
| XhsCreator | ❌ 待检查 | 应统一终端卡片风格 |
| Reviewer | ❌ 待检查 | 应统一终端卡片风格 |
| BookReader | ❌ 待检查 | 应统一终端卡片风格 |
| Books | ❌ 待检查 | 应统一终端卡片风格 |

---

## 6. 交互模式

### 悬停效果
- 卡片: `translateY(-4px)` + `box-shadow` 增强 + 顶部渐变条 `scaleX(1)`
- 链接: 颜色变深 + 下划线
- 按钮: `translateY(-2px)` + 光泽扫过动画

### 动画
- 卡片入场: 打字机效果 (`card-typewriter`, 逐个延迟 0.12s)
- 背景: 代码符号粒子浮动 (`code-float`)
- 光标: 闪烁 (`animate-blink`)
- 页面切换: `cubic-bezier(0.65, 0, 0.35, 1)` 滑动

### 响应式断点
```css
/* Mobile */   @media (max-width: 767px)
/* Tablet */   @media (min-width: 768px) and (max-width: 1023px)
/* Desktop */  默认
/* Large */    @media (min-width: 1440px)
```

---

## 7. 待统一项清单

1. **Dashboard.vue** — 统计卡片和任务列表需改为终端窗口风格
2. **Generate.vue 进度面板** — 添加终端窗口头部（圆点 + 标题）
3. **面包屑组件** — 创建统一的路径风格面包屑 `$ pwd: ~ / page / sub`
4. **XhsCreator / Reviewer / BookReader / Books** — 检查并统一风格
5. **ProgressDrawer** — 头部应使用终端窗口样式而非纯文字
